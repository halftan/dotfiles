---
extends: default

rules:
  # comments-indentation: disable  # don't bother me with this rule
  comments: disable
  document-start: disable
  line-length: disable
    # max: 120
    # level: warning
  indentation:
    indent-sequences: whatever
  truthy:
    allowed-values: ['true', 'false', 'yes', 'no', 'False', 'True']
    check-keys: true
  empty-lines:
    max-end: 1
